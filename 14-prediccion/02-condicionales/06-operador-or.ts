/*
 * EJERCICIO: Operador lógico OR (||)
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let esAdmin = false;
let esModerador = true;
if (esAdmin || esModerador) {
  console.log("Tienes permisos especiales");
}

// Predicción:

export {};
