/*
 * EJERCICIO: Loop con condición múltiple
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let n = 0;
while (n < 10 && n >= 0) {
  console.log(n);
  n += 3;
}

// Predicción:

export {};
