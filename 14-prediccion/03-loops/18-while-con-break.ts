/*
 * EJERCICIO: While con break
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let score = 0;
while (true) {
  score += 10;
  if (score >= 30) {
    break;
  }
  console.log(score);
}
console.log("Final:", score);

// Predicción:

export {};
