/*
 * EJERCICIO: Función con variables locales
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

function calcular() {
  let x = 10;
  let y = 20;
  return x + y;
}
console.log(calcular());

// Predicción:

export {};
