/*
 * EJERCICIO: Función con return
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

function sumar(a: number, b: number) {
  return a + b;
}
let resultado = sumar(5, 3);
console.log(resultado);

// Predicción:

export {};
