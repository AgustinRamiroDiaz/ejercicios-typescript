/*
 * EJERCICIO: Método reverse
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let orden = [1, 2, 3, 4];
orden.reverse();
console.log(orden);

// Predicción:

export {};
