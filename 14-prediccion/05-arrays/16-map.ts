/*
 * EJERCICIO: Método map
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let base = [1, 2, 3];
let dobles = base.map(n => n * 2);
console.log(dobles);
console.log(base);

// Predicción 1:
// Predicción 2:

export {};
