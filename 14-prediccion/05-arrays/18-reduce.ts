/*
 * EJERCICIO: Método reduce
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let cantidades = [1, 2, 3, 4];
let total = cantidades.reduce((acc, val) => acc + val, 0);
console.log(total);

// Predicción:

export {};
