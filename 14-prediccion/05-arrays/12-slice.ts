/*
 * EJERCICIO: Método slice
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let serie = [1, 2, 3, 4, 5];
let porcion = serie.slice(1, 4);
console.log(porcion);
console.log(serie);

// Predicción 1:
// Predicción 2:

export {};
