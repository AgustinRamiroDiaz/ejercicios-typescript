/*
 * EJERCICIO: Método concat
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let arr1 = [1, 2];
let arr2 = [3, 4];
let combinado = arr1.concat(arr2);
console.log(combinado);
console.log(arr1);

// Predicción 1:
// Predicción 2:

export {};
