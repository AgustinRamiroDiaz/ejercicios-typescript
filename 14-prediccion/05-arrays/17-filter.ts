/*
 * EJERCICIO: Método filter
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let edades = [15, 22, 17, 30, 12];
let mayores = edades.filter(edad => edad >= 18);
console.log(mayores);

// Predicción:

export {};
