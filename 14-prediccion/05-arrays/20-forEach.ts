/*
 * EJERCICIO: Método forEach
 *
 * Instrucciones:
 * Escribe tu predicción ANTES de ejecutar el código.
 */

let lista = [10, 20, 30];
lista.forEach(item => {
  console.log(item + 5);
});

// Predicción:

export {};
